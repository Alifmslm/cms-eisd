import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  // Auth logic now handled by Better Auth.
  // This service is retained as a placeholder until removed in cleanup.
}
